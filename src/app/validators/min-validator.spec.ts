import { MinValidator } from './min-validator';

describe('MinValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MinValidator();
    expect(directive).toBeTruthy();
  });
});
